Captured Codex CLI frames for activity detection (ini-lnnk). Codex CLI
version: codex-cli 0.154.0 (the binary eng3 runs; `codex --version`).
Rendered rows as the TUI's emulator shows them (`initech peek eng3 -n 45`),
never hand-drawn.

- idle-frame-dotted.txt — 2026-09-12 ~14:20, eng3 genuinely at rest (waiting
  on qa3; no Working line). Row 42 has the ambient animation's braille dot
  INSIDE the prompt text: "›⠁Ask Codex to do anything". Required fixture row.
- idle-frame.txt — the same pane 1.2s later; six rows differ (the dots moved),
  the prompt row is clean.
- running-frame.txt — Nelson's 2026-09-12 13:40 capture from the bead, verbatim:
  "• Working (2m 40s • esc to int…" above the animated prompt. The animation
  runs while working too; the Working line is the discriminator.
